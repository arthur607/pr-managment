version: '3.1'

services:
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - volumes_db:/var/lib/mongo
    environment:
      - MONGO_INITDB_ROOT_USERNAME=arthur
      - MONGO_INITDB_ROOT_PASSWORD=1qa2ws3ed

volumes:
  volumes_db: